#include<stdio.h>
#include<conio.h>
main(){
	int l,b,area;
	printf("this is to find arer of the rectangel ");
	getch();
	printf("enter lenght ");
	scanf("%d",l);
	getch();
	printf("enter breath ");
	scanf("%d",b);
	getch();
	area= l*b;
	printf("area=%d units",area);
	getch();
	
}
